name: Generate Static Files

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Website Generator
        uses: encreinformatique/static-generator@main
